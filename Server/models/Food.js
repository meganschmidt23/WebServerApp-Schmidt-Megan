const foodList = [
    {name: "Example", calories:12}
];

function addFood(newFood) {
    this.foodList.push(
        {
            food: {name,calories}
        }
    )
}

module.export={
    foodList,
    addFood
}